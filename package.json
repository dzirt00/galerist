{
  "name": "galerist",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@galerist/web",
    "build": "npm run build --workspace=@galerist/web",
    "lint": "npm run lint --workspace=@galerist/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "verify:engine": "npm run typecheck --workspace=@galerist/game-engine && npm run typecheck:test --workspace=@galerist/game-engine && npm run test --workspace=@galerist/game-engine && npm run build --workspace=@galerist/game-engine",
    "verify:web": "npm run lint --workspace=@galerist/web && npm run test --workspace=@galerist/web && npm run build --workspace=@galerist/web",
    "verify": "npm run verify:engine && npm run verify:web"
  },
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10"
  }
}
